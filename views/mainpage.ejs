 <h1>Welcome to the Main Page for Team <%=team%></h1>
<h2>event codes</h2>

<ul>
    <% eventCodes.forEach(eventCode => { %>
      <li>
          <%=eventCode%>
          <ol>
              <% allMatchCodes.forEach(matchArray => { %>
                <% matchArray.forEach(matchKey => { %>
                    <% if (matchKey.includes(eventCode)) { %>
                      <li><%=matchKey%></li>
                    <% } %>
              <% }); %>
            <% }); %>
        </ol>
    </li>
    <% }); %>
  </ul>
  